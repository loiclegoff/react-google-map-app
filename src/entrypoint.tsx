import * as React from 'react'
import { render } from 'react-dom'
// App wrapped with redux Provider, store, etc. (if necessary)

import { App } from './main/App'

render(<App />, document.getElementById('root'))
